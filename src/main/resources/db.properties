# Filtre par Maven au build, selon le profil actif (dev/preprod/prod).
# Ne pas editer target/classes/db.properties : editer ce template.
db.env=${env}
db.url=jdbc:h2:file:./${db.file};AUTO_SERVER=TRUE
db.user=sa
db.password=